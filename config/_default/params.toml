# Pages placed under these sections will be shown on homepage and archive page.
mainSections = ["post"]
description  = "FahdLABs | مختّبر فهد - هنا أشارك كل شيء تقني يشدني: من Windows وLinux إلى AI، الأدوات، المراجعات، وتجارب حقيقية بدون لف ودوران."
# Output page's full content in RSS.
rssFullContent = true
favicon        = "img/favicon-fhd.png"

# Accepted values: "default", "lastmod"
# default = see https://gohugo.io/quick-reference/glossary/#default-sort-order
# lastmod = sort by last modified date, in descending order
SortBy              = "default"

[footer]
    since      = 2026
    customText = "FahdLABs - تقنية على السريع وبأسلوب بسيط: نجرب، نضبط، ونشارك اللي ينفع."

[dateFormat]
    published   = ":date_full"
    lastUpdated = ":date_full"

[sidebar]
    emoji    = "👺"
    subtitle = "يا هلا! هذه مدونة FahdLABs. كل ما يحمّسني في التقنية بتحصله هنا: Windows، Linux، AI، مراجعات، أدوات، وتجارب عملية من الواقع."
    avatar   = "img/avatar-fhd.png"

[article]
    headingAnchor = false
    math          = false
    readingTime   = true

    [article.license]
        enabled = true
        default = "مرخّص تحت CC BY-NC-SA 4.0"

[widgets]
    homepage = [
        { type = "search" },
        # { type = "archives", params = { limit = 5 } },
        { type = "categories", params = { limit = 10 } },
        { type = "tag-cloud", params = { limit = 10 } },
    ]
    page = [{ type = "toc" }]

[opengraph.twitter]
    site = "@fahdlabs"
    card = "summary_large_image"

[colorScheme]
    toggle  = true
    default = "auto"

## Comments
[comments]
    enabled  = true
    provider = "disqus"

    # See all the available configurations at https://github.com/CaiJimmy/hugo-theme-stack/blob/master/config/_default/params.toml
    # Copy the configurations you need from there and paste it here.
